# --- BASE DE DATOS ---
# La conexión la inyecta Testcontainers (@ServiceConnection): no va URL acá.

# --- MIGRACIONES Y ESTRUCTURA ---
# El schema lo crea Flyway, igual que en producción.
spring.flyway.enabled=true
# Solo V1 (schema). V2 carga datos iniciales que chocarían con los datos de los tests.
spring.flyway.target=1
# Hibernate ya no crea tablas: valida que las entidades coincidan con el schema real.
spring.jpa.hibernate.ddl-auto=validate

# --- SEGURIDAD: JWT ---
app.jwt.secret=mi-super-secret-key-ultra-larga-123456789012345678901234567890
app.jwt.expiration=900000
app.jwt.refresh-expiration=604800000

# --- SEGURIDAD: OAUTH2 GOOGLE (Mock) ---
spring.security.oauth2.client.registration.google.client-id=mock-id
spring.security.oauth2.client.registration.google.client-secret=mock-secret
spring.security.oauth2.client.registration.google.scope=email,profile